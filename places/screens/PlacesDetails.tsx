import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

type OwnProps = {}
const PlacesDetails: React.FC<OwnProps> = () => {
  return (
    <View>
      <Text>places tailes</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PlacesDetails;
