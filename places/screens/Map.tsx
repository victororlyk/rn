import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

type OwnProps = {}
const Map:React.FC<OwnProps> = () => {
  return (
      <View>
        <Text>amp</Text>
      </View>
  );
};

const styles = StyleSheet.create({});

export default Map;
